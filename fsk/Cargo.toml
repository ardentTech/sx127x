[package]
name = "sx127x-fsk"
version = "0.1.0"
edition = "2024"
description = "A no-std driver for the FSK modem on Semtech Sx127x chips."

[dependencies]
embedded-hal-async = "1.0.0"
sx127x-common = { path = "../common" }

[dev-dependencies]
approx = "0.6.0-rc2"